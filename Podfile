# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PictureAnnotate' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for PictureAnnotate

  target 'PictureAnnotateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PictureAnnotateUITests' do
    inherit! :search_paths
    # Pods for testing
  end
pod "MJExtension"
pod "SVProgressHUD"
pod 'Masonry'
pod 'IQKeyboardManager'
end
